# CLAUDE.md

このファイルは、このリポジトリで作業するClaude Code(および今後のセッション)向けのガイドです。

## プロジェクト概要

飼育している生態(魚・爬虫類・昆虫等)と水槽/ケージの情報を管理するWebアプリケーション「AquaLife Manager」。

主要機能:
1. 水槽/ケージ管理(サイズ、水量、レイアウト情報)
2. 生態管理(種、個体、導入日、体調記録の履歴)
3. カレンダー(給餌日・清掃日の確認。外部カレンダー連携は不要と決定し未実装)
4. 自動スケジューリング(水槽サイズ・水量・収容生体数から清掃日を自動算出)
5. 健康管理(体調記録・飼育密度から、ルールベースで健康状態の目安を分析。外部AI APIは不使用)
6. Supabase Authによるログイン認証(未ログインではアプリ本体・DBに一切アクセス不可)

詳細な要件は `docs/requirements.md`、開発ステップは `docs/flow.md` を参照。

## 開発フロー(全13ステップ)

`docs/flow.md` に進捗チェックリストがある。**このファイルは各ステップ完了時に必ず更新すること。**

現在の状況(2026-08-21時点): 全13ステップ完了。GitHubへpush済み、Vercelへデプロイ済み(認証で保護)。
今後は`docs/flow.md`の「今後の拡張候補」を参照し、追加要望ベースで機能拡張を行う。

## 技術スタック

- フロントエンド: Next.js (App Router) + TypeScript + Tailwind CSS
- PWA対応
- データベース: Supabase (PostgreSQL)。認証はSupabase Auth(メール+パスワード、単一ユーザー想定)
- パッケージマネージャ: npm
- デプロイ: Vercel(GitHub連携による自動デプロイ)
- ローカルでのワンクリック起動: `start-aqualife-manager.command`(macOS)

## 開発方針・規約

- コミットメッセージは日本語で、変更内容の「なぜ」を簡潔に書く
- 新機能を追加したら `docs/flow.md` のチェックリストを更新する
- UIコンポーネントは可能な限り再利用可能な単位に分割する
- 環境変数(DB接続情報など)は `.env.local` に置き、コミットしない(`.gitignore` 済み)
- テストや動作確認の手順は各機能実装時にこのファイルまたは `docs/` に追記していく

## 決定事項(すべてdocs/requirements.mdに記録済み)

- 外部カレンダー連携: 不要(アプリ内カレンダーのみ)
- 対象生態: 魚・爬虫類・昆虫など幅広く対応
- マルチユーザー対応: 不要(個人利用のみ)。ただしログイン認証は実装(公開URLへの無制限アクセスを防ぐため)
- データベース: Supabase(PostgreSQL)
- AI健康管理: 外部AI APIは使わず、ルールベースのヒューリスティック分析

## 作業ルール

- 新規実装時は、必ず新規ブランチを作成してから作業すること(mainブランチへの直接コミットは行わない)
- PR作成時は、必ずユーザーの承認チェックを行うこと(無断でマージしない)
- 不確定要素が残ったまま実装を進めないこと。着手前にユーザーへ確認し、決定事項として記録してから実装すること
- 以降の返答は日本語で行うこと
