# 制作フロー

水槽・生態管理システム開発の全体フロー。各ステップ完了後にチェックを入れる。

- [x] 1. フォルダの用意
- [x] 2. バージョン管理システムとの同期(ローカルgit初期化。リモートはユーザー側でGitHubリポジトリを作成し接続)
- [x] 3. VSCodeフォルダの追加
- [x] 4. CLAUDE.mdの追加
- [x] 5. 基盤システムの作成(Next.js + TypeScript + Tailwind CSSの雛形構築、PWA対応)
- [x] 6. カレンダーの作成(給餌日・清掃日を表示するアプリ内カレンダーUI。外部カレンダー連携は不要のため対象外)
- [x] 7. ユーザーが追加する情報(レイアウト情報、生態情報)の定義・保存(暫定でlocalStorageに保存。DB接続はステップ8)
- [x] 8. データベースとの接続・同期・更新(Supabase接続。ユーザー環境で実データの読み書きを確認済み)
- [x] 9. 現段階でアプリケーションとしての起動、テスト確認(重大なService Workerの不具合を発見・修正)
- [x] 10. 自動スケジューリングシステムの追加実装(水槽/ケージのサイズ・水量・収容生体数から清掃日を自動算出し、カレンダーに実データを反映)
- [x] 11. 生態、水槽内の情報から健康管理AIを搭載実装(外部AI APIは使わず、ルールベースのヒューリスティック分析で実装)
- [x] 12. アプリケーションの起動、テスト(認証機能を追加し、ログイン保護込みで動作確認)
- [ ] 13. マスターアップ

## 決定事項(2026-08-21時点)

- プラットフォーム: Webアプリ(PWA)
- バージョン管理: 新規GitHubリポジトリを作成する方針。ただし本セッションの環境からは認証済みのGitHub連携手段がないため、ローカルgitで管理し、リモート接続はユーザー側で実施する(手順は README.md 参照)。
- データベース: 未定。要件(複数水槽・複数生態の管理、カレンダー同期、将来のAI健康管理機能)から、リレーショナルなクラウドDB(Supabase/PostgreSQL)を提案。詳細はdocs/requirements.mdの「データベース方針」を参照。

## 決定事項(2026-08-21 ヒアリング、ステップ5着手前)

- 外部カレンダー連携: 不要(アプリ内カレンダーのみ)
- 対象生態: 魚・爬虫類・昆虫など幅広く対応
- マルチユーザー対応: 不要(個人利用のみ)

詳細はdocs/requirements.mdの「決定事項」を参照。

## ステップ5 完了メモ(2026-08-21)

- `create-next-app`(App Router / TypeScript / Tailwind CSS / ESLint / src ディレクトリ)で雛形を構築
- PWA対応として `public/manifest.json`・`public/sw.js`(Service Worker)・`public/icons/icon.svg` を追加し、`src/app/layout.tsx` にmanifest/theme-colorを設定、`src/components/ServiceWorkerRegister.tsx` で登録
- トップページ(`src/app/page.tsx`)を主要機能5項目を紹介する内容に置き換え
- `npm run build`(Lint + 型チェック + ビルド)、ローカルdevサーバーでの表示確認、manifest/Service Worker登録の動作確認まで完了
- ブランチ: `feature/step5-foundation`(ユーザー承認のうえmainにローカルマージ済み)

## ステップ6 完了メモ(2026-08-21)

- 月表示のアプリ内カレンダー(`/calendar`)を実装。前月・次月移動、日付選択で給餌・清掃の予定一覧を表示
- 型定義 `src/types/schedule.ts`、月グリッド生成 `src/lib/calendar.ts`、サンプルデータ生成 `src/lib/mock-schedule.ts` を追加
- 生態・水槽データの登録・保存機能(ステップ7)およびDB接続(ステップ8)が未実装のため、現段階はモックデータで表示。実データ実装後に `mock-schedule.ts` を実データ取得に置き換える
- 外部カレンダー連携は決定事項により対象外
- `npm run build`(Lint + 型チェック + ビルド)、devサーバーでの表示・月送り・日付選択の動作確認済み
- ブランチ: `feature/step6-calendar`(ユーザー承認のうえmainにローカルマージ済み)

## ステップ7 完了メモ(2026-08-21)

- 水槽/ケージ管理(`/tanks`)と生態管理(`/creatures`)の登録・編集・削除UIを実装
- 型定義 `src/types/tank.ts`・`src/types/creature.ts`
- 永続化は暫定でlocalStorageを使用(`src/lib/use-local-collection.ts`、`useSyncExternalStore`ベース)。UIからは同じ戻り値の形(items/upsert/remove)のみに依存する設計とし、ステップ8でDB接続に差し替える際もUI側の変更を最小限にできるようにした
- 生態管理では所属する水槽/ケージをプルダウンで選択可能(tanksコレクションを参照)
- ホームに各機能への導線を追加(未実装の自動スケジューリング・AI健康管理は準備中表示)
- 開発中に発見した不具合を修正: `useSyncExternalStore`のgetServerSnapshotが毎回新しい配列を返しSSR/クライアントでハイドレーション不整合を起こしていた点、および無限ループ警告。開発モードのFast Refresh特有の表示ずれ(本番ビルドでは非再現)も別途確認済み
- `npm run build`(Lint + 型チェック + ビルド)に加え、本番モード(`npm start`)でも実際にブラウザから水槽・生態の追加/編集/削除・画面間の連携(生態→所属水槽の反映)を確認
- ブランチ: `feature/step7-data-entry`(ユーザー承認のうえmainにローカルマージ済み)

## ステップ8 完了メモ(2026-08-21)

- データベース方針をSupabase(PostgreSQL)に確定(ユーザー承認済み)
- `supabase/schema.sql` にtanks/creaturesテーブルとRLSポリシー(個人利用・anon key前提の暫定ポリシー)を定義
- `src/lib/supabase/client.ts` でSupabaseクライアントを作成(環境変数未設定時はエラーで即失敗する設計)
- `src/lib/supabase/use-supabase-table.ts` を汎用フックとして実装。初回マウント時にサーバーから取得し、追加/更新/削除の直後はローカルのitemsを直接更新する(楽観的更新)方式を採用
- `src/lib/supabase/tanks.ts`・`creatures.ts` でDB行(snake_case)とアプリ側の型(camelCase)を変換するアダプタを実装し、`TankManager`・`CreatureManager`をlocalStorage版からSupabase版に置き換え
- 不要になった `src/lib/use-local-collection.ts`・`src/lib/storage-keys.ts` を削除
- `.env.local.example` を追加、READMEにSupabaseセットアップ手順を追記
- ユーザーがSupabaseプロジェクトを作成・`supabase/schema.sql`実行・`.env.local`設定を実施。設定完了後、実際のブラウザから水槽/生態の追加・編集・削除、画面間連携(生態→所属水槽)を確認
- 開発中に発見した不具合と対応:
  - Project URLに`/rest/v1/`が誤って付与されていたことによる接続エラーをユーザーとともに特定・修正
  - 当初実装していた`postgres_changes`のリアルタイム購読が、書き込み直後の再取得を不安定にする要因になっていたため撤去し、書き込み成功後はローカル状態を直接更新する方式に変更して安定性を確保(個人利用のみのため複数端末間リアルタイム同期は現状不要と判断)
  - devモードのFast Refreshが進行中のリクエストを中断し表示が停止する現象を確認。本番ビルド(`npm start`)でのクリーンな検証により、通常利用では問題ないことを確認
- ブランチ: `feature/step8-database`(ユーザー承認のうえmainにローカルマージ済み)

## ステップ9 完了メモ(2026-08-21)

アプリケーション全体(トップ・カレンダー・水槽/ケージ管理・生態管理)を本番ビルドで
通しで動作確認した。この過程で重大な不具合を発見・修正した。

- **重大バグ**: `public/sw.js`のService Workerが、GETリクエストであれば
  同一オリジンか外部APIかを問わず一律キャッシュしていたため、Supabaseへの
  REST APIリクエスト(水槽/生態の一覧取得)まで古いレスポンスを返し続けていた。
  これがステップ8での「更新・削除しても画面に反映されない」現象の真因だった。
  同一オリジンのリクエストのみキャッシュ対象とするよう修正し、キャッシュ名も
  `aqualife-cache-v2`に更新して旧キャッシュを破棄させるようにした
- 追加で、DB書き込み失敗時にフォーム入力内容が失われる不具合(成功可否を待たずに
  フォームをリセットしていた)を発見・修正。`upsert`/`remove`が成功可否を返すよう
  `use-supabase-table.ts`を変更し、`TankManager`/`CreatureManager`は成功時のみ
  フォームをリセットするようにした
- 修正後、本番ビルド・新規タブでの読み込みで水槽/生態の追加・編集・削除が
  即座かつ正しく反映されることを再確認
- ブランチ: `feature/step9-integration-test`(ユーザー承認のうえmainにローカルマージ済み)

## ステップ10 完了メモ(2026-08-21)

- `src/lib/cleaning-schedule.ts`: 水槽/ケージの種別・水量・収容生体数から清掃間隔(日)を算出する
  `calculateCleaningIntervalDays`を実装。水量は平方根で緩やかに、生体数は多いほど間隔を縮めるロジック
  (詳細な計算根拠はファイル内コメント参照)
- `src/lib/use-schedule-events.ts`: Supabaseの実データ(tanks/creatures)から指定月の給餌日・清掃日を
  算出するフックを実装。清掃日は上記アルゴリズム、給餌日は生体分類ごとの基準間隔(魚:1日、爬虫類:3日、
  昆虫:2日、その他:2日)を使用し、水槽の登録日・生体の導入日を起点に間隔通りの日付を列挙する
- `MonthCalendar`をモックデータ(`mock-schedule.ts`、削除済み)から実データに切り替え。水槽/生態が
  未登録の場合は登録ページへの案内を表示
- 本番ビルドで実際に水槽(60L)・生態を登録し、清掃間隔が計算通り(この条件では11日間隔)に
  カレンダー上へ反映されることを確認済み
- ブランチ: `feature/step10-auto-scheduling`(ユーザー承認のうえmainにローカルマージ済み)

## ステップ11 完了メモ(2026-08-21)

- 方針決定(ユーザーヒアリング): 外部AI APIは使用せず、ルールベースのヒューリスティック分析で実装。
  分析精度向上のため、日付付きの体調記録(履歴)機能を先に追加
- DBに`creature_logs`テーブルを追加(`supabase/schema.sql`。creature_id経由でcreatures削除時にcascade削除)。
  ユーザーがSupabaseのSQL Editorで実行し、動作確認まで実施
- `src/components/creatures/CreatureLogSection.tsx`: 生態管理画面の各生体カードに、
  日付+メモの体調記録を追加・一覧表示・削除できる折りたたみセクションを追加
- `src/lib/health-analysis.ts`: ルールベースの健康状態評価ロジックを実装。
  - キーワード検出(「拒食」「元気がない」等は要注意、「痩せ」等は要観察)
  - 体調記録が30日以上更新されていない場合は要観察
  - 水槽(aquarium)の場合、収容生体数に対して水量が少ない(目安: 1匹あたり5L未満)場合は
    過密飼育の注意を要観察として表示
- `/health` ページ(`HealthDashboard`)で、登録済みの生態ごとに健康状態(良好/要観察/要注意)と
  判定理由を一覧表示。要注意→要観察→良好の順にソート
- ホームの「AI健康管理」カードを「健康管理」に更新し`/health`へリンク。
  「自動スケジューリング」カードも実装済みとして`/calendar`へリンク
- 本番ビルドで実際に水槽(8L・2匹)と体調記録(拒食キーワードを含む記録)を登録し、
  「要注意」「要観察」の判定・過密飼育の警告が期待通り表示されることを確認済み
- ブランチ: `feature/step11-health-ai`(ユーザー承認のうえmainにローカルマージ済み)

## ステップ12 完了メモ(2026-08-21)

ユーザーからの追加要望により、公開URLへのデプロイを見据えた認証機能を実装した
(「URLを知っている人間のみアプリを開けるようにしたい」という要件)。

- Supabase Authによるメール+パスワード認証を実装。`src/components/auth/AuthGate.tsx`が
  アプリ全体をラップし、未ログインの場合はログインフォームのみを表示(アプリ本体は一切見せない)
- アプリ内に公開のサインアップ画面は設けていない。ユーザーアカウントはSupabaseダッシュボードの
  Authentication > Usersから作成する運用とした(誰でも自由にアカウントを作れないようにするため)
- `supabase/schema.sql`のRLSポリシーを、個人利用・未認証(anon key)前提の暫定設定から
  `auth.uid() is not null`(ログイン済みユーザーのみ許可)に変更
- 動作確認: 未認証でのDB書き込みが401(row-level security policy違反)で拒否されることを
  直接APIリクエストで確認。ユーザー自身がSupabaseでアカウントIDを作成し、ブラウザでログイン、
  ログイン後に水槽データの追加が正常に行えることを確認済み
- ログインフォームのエラーメッセージを、Supabaseのエラーコード(メール未確認/認証情報不正/その他)に
  応じて具体的に表示するよう改善
- ワンクリック起動用の `start-aqualife-manager.command` を追加(ダブルクリックで
  初回ビルド→サーバー起動→ブラウザ表示まで自動化)。実際に実行しビルド・起動・応答まで確認済み
- ブランチ: `feature/step12-auth-deploy`(未マージ、ユーザー承認後にマージ予定)
